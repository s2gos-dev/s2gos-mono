# Configuration

The simulator reads from the same `s2gos_settings.yaml` file used across the monorepo.
Shared settings (`common:` section) are documented in the
[s2gos-utils Configuration](../../s2gos-utils/docs/configuration.md) page.
